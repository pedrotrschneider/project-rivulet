meta {
  name: Config
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/user/config
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "real_debrid_key": "{{rdApiKey}}",
    "tmdb_key": "{{tmdbApiKey}}",
    "mdblist_key": "{{mdblistApiKey}}",
    "aiostream_url": "{{aiostreamUrl}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
