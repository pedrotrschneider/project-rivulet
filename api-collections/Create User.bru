meta {
  name: Create User
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{devEmail}}",
    "password": "{{devPassword}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
