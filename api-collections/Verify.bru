meta {
  name: Verify
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/auth/verify
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{devEmail}}",
    "code": "171145"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
