meta {
  name: Add Profile
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/profiles
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Kids"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
